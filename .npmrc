# Force "file:" references to be installed as directory copies rather than
# symlinks (npm 7+ default). Turbopack's module resolver cannot follow
# symlinks that resolve outside the declared project root, so packages in
# the sibling sdk/ workspace must land in node_modules as real directories.
install-links=true
